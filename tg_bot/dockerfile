FROM python:3.11

ARG POSTGRES_HOST
ARG POSTGRES_PORT
ARG POSTGRES_DB
ARG POSTGRES_USER
ARG POSTGRES_PASSWORD

ARG API_HOST
ARG API_PORT

ARG TG_TOKEN_BOT

WORKDIR /app

COPY . /app

RUN pip install -r requirements.txt

ENV PYTHONPATH=/app

ENTRYPOINT python3 /app/main.py